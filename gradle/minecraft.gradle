minecraft {
    forgeVersion = clientForgeVersion
    runDir = 'run'
    mappings = mcpMappings
    makeObfSourceJar = false
}

if (shadowJar != null) {
    reobf {
        shadowJar {
            mappingType = 'SEARGE'
        }
    }
}

sourceJar {
    group = 'build'
}

artifacts {
    archives sourceJar
}