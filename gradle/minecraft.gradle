minecraft {
    forgeVersion = clientForgeVersion
    runDir = 'run'
    mappings = mcpMappings
    makeObfSourceJar = false
}

reobf {
    shadowJar {
        mappingType = 'SEARGE'
    }
}

sourceJar {
    group = 'build'
}

artifacts {
    archives sourceJar
}