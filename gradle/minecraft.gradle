minecraft {
    forgeVersion = clientForgeVersion
    runDir = 'run'
    mappings = mcpMappings
    makeObfSourceJar = false
}

sourceJar {
    group = 'build'
}

artifacts {
    archives sourceJar
}

if (project.tasks.findByPath(":shadowJar") != null) {
    reobf {
        shadowJar {
            mappingType = 'SEARGE'
        }
    }
}
