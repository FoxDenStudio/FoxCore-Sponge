minecraft {
    forgeVersion = clientForgeVersion
    runDir = 'run'
    mappings = mcpMappings
    makeObfSourceJar = false
}

reobf {
    if (shadowJar != null) {
        shadowJar {
            mappingType = 'SEARGE'
        }
    }
}

sourceJar {
    group = 'build'
}

artifacts {
    archives sourceJar
}