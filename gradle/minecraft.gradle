minecraft {
    forgeVersion = clientForgeVersion
    runDir = 'run'
    mappings = mcpMappings
    makeObfSourceJar = false
}

sourceJar {
    group = 'build'
}

artifacts {
    archives sourceJar
}

if (tasks.findByPath(":shadowJar") != null) {
    reobf {
        shadowJar {
            mappingType = 'SEARGE'
        }
    }
}
